#ifndef MATHS_H
#define MATHS_H

#include <math.h>

int max(int a, int b);
int min(int a, int b);

float absValue(float x);

int factorial(int n);

int gcd(int a, int b);
int lcm(int a, int b);

#endif
